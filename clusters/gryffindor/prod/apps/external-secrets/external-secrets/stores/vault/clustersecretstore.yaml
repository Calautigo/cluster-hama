apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "https://api.k8s.gryffindor.prod.schwarz47.at:6443"
      path: "kv"
      version: "v1"
      auth:
        tokenSecretRef:
          name: vault-token
          key: token