apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: newt
resources:
  - externalsecret.yaml
  - helmrepository.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: newt-values
    files:
      - values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml