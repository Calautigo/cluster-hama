apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zitadel-route
  namespace: security
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: https
  hostnames:
  - "sso.schwarz47.at"
  rules:
  - backendRefs:
    - name: zitadel
      port: 8080
