---
# yaml-language-server: $schema=https://raw.githubusercontent.com/aclerici38/pocket-id-operator/main/dist/schemas/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: vikunja
spec:
  name: Vikunja
  allowedUserGroups:
    - name: hama
      namespace: security
  launchUrl: https://vikunja.ds47.dev
  callbackUrls:
    - https://vikunja.ds47.dev/oauth/oidc/callback
  secret:
    name: vikunja-secret
    keys:
      clientID: VIKUNJA_AUTH_OPENID_PROVIDERS_POCKETID_CLIENTID
      clientSecret: VIKUNJA_AUTH_OPENID_PROVIDERS_POCKETID_CLIENTSECRET
      issuerUrl: VIKUNJA_AUTH_OPENID_PROVIDERS_POCKETID_AUTHURL
