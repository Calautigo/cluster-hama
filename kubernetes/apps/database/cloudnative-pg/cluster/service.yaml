---
# yaml-language-server: $schema=https://schemas.zinn.ca/v1/service.json
apiVersion: v1
kind: Service
metadata:
  name: &app postgres-db
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
  annotations:
    external-dns.alpha.kubernetes.io/hostname: postgres-db.schwarz47.at
    lbipam.cilium.io/ips: 10.100.10.210
spec:
  type: LoadBalancer
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    cnpg.io/cluster: postgres
    role: primary
