---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app influxdb
  namespace: database
spec:
  interval: 5m
  chart:
    spec:
      chart: influxdb2
      interval: 5m
      sourceRef:
        kind: HelmRepository
        name: influxdata-charts
      version: 2.1.2
  values:
    fullnameOverride: *app
    image:
      repository: influxdb
      tag: 2.9.1-alpine
    resources:
      requests:
        cpu: 30m
        memory: 512Mi
      limits:
        memory: 4096Mi
    adminUser:
      create: true
      organization: hamavil
      bucket: default
      user: "admin"
      retention_policy: "0s"
      existingSecret: influxdb-secret
    # influxd runs as uid/gid 2000 to match the volsync/kopiur mover default
    securityContext:
      runAsUser: 2000
      runAsGroup: 2000
      runAsNonRoot: true
    pdb:
      create: false
    service:
      type: LoadBalancer
      annotations:
        external-dns.alpha.kubernetes.io/hostname: influxdb.schwarz47.at
        lbipam.cilium.io/ips: "${LB_IP_INFLUXDB}"
    persistence:
      enabled: true
      useExisting: true
      name: influxdb-data
