---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: externaldns-technitium
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: externaldns-technitium
  values:
    fullnameOverride: externaldns-technitium
    logLevel: info
    podAnnotations:
      secret.reloader.stakater.com/reload: externaldns-technitium-secret
    env:
      - name: EXTERNAL_DNS_RFC2136_TSIG_SECRET
        valueFrom:
          secretKeyRef:
            name: externaldns-technitium-secret
            key: EXTERNAL_DNS_RFC2136_TSIG_SECRET
    extraArgs:
      - --rfc2136-host=10.10.10.32
      - --rfc2136-port=53
      - --rfc2136-zone=ds47.dev
      - --rfc2136-tsig-secret-alg=hmac-sha256
      - --rfc2136-tsig-keyname=external-dns
      - --rfc2136-tsig-axfr
    policy: sync
    provider: rfc2136
    serviceMonitor:
      enabled: true
    sources:
      - gateway-httproute
      - gateway-tlsroute
      - service
    txtOwnerId: default
    txtPrefix: k8s.
