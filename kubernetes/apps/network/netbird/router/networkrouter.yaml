---
apiVersion: netbird.io/v1alpha1
kind: NetworkRouter
metadata:
  name: axon
spec:
  # The DNS zone must exist in the NetBird dashboard (DNS -> Zones) before
  # the operator can register records in it.
  dnsZoneRef:
    name: ds47.dev
