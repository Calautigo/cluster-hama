---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: cluster-hama-vault
      auth:
        secretRef:
          secretAccessKeySecretRef:
            key: secret-access-credentials
            name: gcp-credentials