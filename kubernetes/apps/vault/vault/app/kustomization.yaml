---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrepository.yaml
  - ./helmrelease.yaml
  - ./httproute.yaml
  - ./servicemonitor.yaml
  - ./prometheusrule.yaml
  - ./snapshot-cronjob.yaml
  # Add after `vault operator init` + key encryption:
  # - ./unsealer-secret.sops.yaml
  # - ./unsealer.yaml
