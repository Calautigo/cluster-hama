---
# yaml-language-server: $schema=https://raw.githubusercontent.com/aclerici38/pocket-id-operator/main/dist/schemas/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: audiobookshelf
spec:
  name: Audiobookshelf
  allowedUserGroups:
    - name: hama
      namespace: security
    - name: media
      namespace: security
  launchUrl: https://audiobookshelf.ds47.dev
  callbackUrls:
    - https://audiobookshelf.ds47.dev/auth/openid/callback
  secret:
    name: audiobookshelf-oidc
    keys:
      clientID: OIDC_CLIENT_ID
      clientSecret: OIDC_CLIENT_SECRET
      issuerUrl: OIDC_ISSUER_URL
