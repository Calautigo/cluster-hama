apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rook-ceph-https-route
  namespace: rook-ceph
spec:
  parentRefs:
  - name: cilium-gateway
    namespace: network
  hostnames:
  - "rook.schwarz47.at"
  rules:
  - backendRefs:
    - name: rook-ceph-mgr-dashboard
      port: 7000
