---
terminal:
  backend: local
  cwd: /opt/data/workspace
  persistent_shell: true

memory:
  memory_enabled: true

compression:
  enabled: true
  threshold: 0.5
  target_ratio: 0.2
  protect_last_n: 20

web:
  search_backend: searxng
