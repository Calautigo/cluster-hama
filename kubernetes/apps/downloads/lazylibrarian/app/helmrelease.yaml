---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app lazylibrarian
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: lazylibrarian
  dependsOn:
    - name: ceph-csi-rbd
      namespace: storage
  values:
    controllers:
      lazylibrarian:
        pod:
          securityContext:
            # linuxserver s6 image: init must run as root; the app itself drops
            # to PUID/PGID (2000). Do not drop capabilities - s6-setuidgid and
            # lsiown need SETUID/CHOWN to manage the abc user.
            runAsUser: 0
            runAsGroup: 0
            runAsNonRoot: false
            fsGroup: 2000
            fsGroupChangePolicy: OnRootMismatch

        containers:
          app:
            image:
              repository: ghcr.io/linuxserver/lazylibrarian
              tag: version-42fc6140@sha256:eb74dfe59b7703b1129a4be568c16bf1bfd9925338c7ed7167f8910bb41112a4
            env:
              PUID: "2000"
              PGID: "2000"
              TZ: "${TZ}"
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: &port 5299
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            resources:
              requests:
                cpu: 20m
                memory: 256Mi
              limits:
                memory: 1Gi
            securityContext:
              allowPrivilegeEscalation: false

    service:
      app:
        controller: *app
        ports:
          http:
            port: *port

    route:
      app:
        hostnames:
          - lazylibrarian.ds47.dev
        parentRefs:
          - name: kgateway-internal
            namespace: network

    persistence:
      config:
        existingClaim: lazylibrarian-config
        globalMounts:
          - path: /config
      books:
        type: nfs
        server: 10.100.0.81
        path: /media/media/media/books
        globalMounts:
          - path: /books
      audiobooks:
        type: nfs
        server: 10.100.0.81
        path: /media/media/media/audiobooks
        globalMounts:
          - path: /audiobooks
      downloads:
        type: nfs
        server: 10.100.0.81
        path: /media/media/usenet/complete
        globalMounts:
          - path: /downloads
