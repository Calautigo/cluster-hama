apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: goldilocks
spec:
  parentRefs:
  - name: cilium-gateway
  hostnames:
  - goldilocks.${SECRET_DOMAIN}
  rules:
  - backendRefs:
    - name: goldilocks-dashboard
      port: 80